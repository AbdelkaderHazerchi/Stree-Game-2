# مخطط أماكن رسم العناصر

هذا الملف يحدد كل قسم رسم رئيسي في `index.html`، موقعه داخل الملف (أسطر)، وإرشادات مختصرة لتغيير الرسم بأمان.

**ملاحظات عامة**
- الملف الرئيسي: [index.html](index.html)
- دالة الرندر الرئيسية: [index.html](index.html#L34629-L35356)

**أقسام الرسم**

- **الطTiles (بنايات، طرق، رصيف، حدائق، مواقف، مبانٍ خاصة):**
  - مكان في الملف: [index.html](index.html#L34647-L34855)
  - الوصف: رسم كل البلاطات داخل الحلقة الرئيسية في `render()` عبر `switch (tile)`.
  - للتغيير الآمن: عدّل فقط أجزاء `case T.BUILDING`, `case T.ROAD`, `case T.SIDEWALK`, `case T.PARK`, `case T.PARKING`, `case T.SPECIAL`. احتفظ بنسخة من الكود قبل الاستبدال.

- **مؤشرات المهام (Mission givers):**
  - مكان في الملف: [index.html](index.html#L34856-L34940)
  - الوصف: نقاط صفراء متوهجة ورسائل صغيرة. تُرسم كدوائر ونصوص.
  - للتغيير الآمن: استبدل الأشكال بتصاميم مصغرة باستخدام `ctx.drawImage` بعد تحميل الصور.

- **الـ NPCs (المشاة):**
  - مكان في الملف: [index.html](index.html#L34941-L35014)
  - الوصف: رسم الظل، الأرجل، الجسم، الرأس، واللمسات الخاصة (عصابة/سلاح).
  - للتغيير الآمن: ضع رسم جديد داخل نفس الـ `for (const npc of npcs)`، أو اترك الرسوم القديمة مع تعليقها للرجوع.

- **اللووت (للمواد/الذخيرة/النقود):**
  - مكان في الملف: [index.html](index.html#L35015-L35051)
  - الوصف: نقاط متوهجة وصناديق صغيرة تُظهر نوع وكمية.
  - للتغيير الآمن: استبدال الشرائط/الدوائر بصور صغيرة بعد تحميلها.

- **المركبات (السيارات):**
  - مكان في الملف: [index.html](index.html#L35052-L35167)
  - الوصف: رسم الجسم، العجلات، المصابيح، وعلامات الشرطة إن وُجدت.
  - للتغيير الآمن: الأفضل إنشاء صور مركبة بأبعاد `v.w` و`v.h` ثم رسمها مركزية:

```js
// تحميل مرة واحدة أثناء الإعداد
const SPRITES = { car: new Image() };
SPRITES.car.src = 'assets/sprites/car.png';

// داخل حلقة الرسم للسيارة استبدال المسارات بالشكل التالي:
ctx.save();
ctx.translate(v.x, v.y);
ctx.rotate(v.angle);
ctx.drawImage(SPRITES.car, -v.w/2, -v.h/2, v.w, v.h);
ctx.restore();
```

- **الشرطة (مشى على الأقدام):**
  - مكان في الملف: [index.html](index.html#L35168-L35224)
  - الوصف: رسم مماثل للـ NPC مع قبعة/شارة.
  - للتغيير الآمن: مثل الـ NPCs، استبدل الجسم بصورة أو أيقونة.

- **الطلقات (Bullets):**
  - مكان في الملف: [index.html](index.html#L35225-L35271)
  - الوصف: مستطيلات صغيرة مع توهج. تغيير سريع عبر تغيير ألوان أو استبدال بصورة صغيرة.

- **اللاعب (Player):**
  - مكان في الملف: [index.html](index.html#L35272-L35355)
  - الوصف: رسم الظل، الأرجل، الجسم، الرأس، وسهم الاتجاه. عند وجود مركبة يُظهر تمييز حول المركبة.
  - للتغيير الآمن: عدّل داخل كتلة `if (player.onFoot)` أو استبدل بالرسم بصور (كما في مثال المركبة)، واحتفظ بنسخة قديمة.

- **الخريطة المصغرة (Minimap):**
  - مكان في الملف: [index.html](index.html#L35357-L35480)
  - الوصف: دالة `renderMinimap()`، ترسم خلفية المناطق، البلاطات، نقاط اللاعب، الشرطة، والمهام.
  - للتغيير الآمن: عدّل ألوان الخريطة أو أضف أيقونات مصغرة عبر `mc.drawImage(...)` بعد تحميل صور مناسبة.

**نصائح عامة لتغيير الرسوم بأمان**

1. عمل نسخة احتياطية: قبل أي تعديل، احفظ نسخة من `index.html` أو استخدم Git commit.
2. استخدم صورًا محمّلة مسبقًا: أنشئ كائن `SPRITES` في أعلى السكربت، حمّل الصور قبل بدء اللعبة.
3. لا تغيّر المنطق الفيزيائي: حافظ على مراكز الرسم (عادةً translate إلى `x,y`) وأبعاد الواح `v.w, v.h` و`p.size` لعدم كسر الاصطدامات.
4. اختبر محليًا: افتح `index.html` في المتصفح، راقب الـ Console للأخطاء، وجرب حالات مختلفة (مشاة، سيارات، مطاردة شرطة).
5. استخدم تعليقات بدلاً من حذف: علّق الكود القديم مؤقتًا لتسهيل التراجع.
6. قم بتغييرات تدريجية: غيّر عنصرًا واحدًا ثم اختبر بدلاً من استبدال مجموعة كبيرة دفعة واحدة.
7. أمثلة سريعة: استبدال رسم بسيط بصورة، كما في مثال `drawImage` للمركبة أعلاه.

إذا تريد، أستطيع:
- إنشاء مجلد `assets/sprites/` وإضافة مثال صورة PNG/placeholder.
- تطبيق مثال `drawImage` واحد (مثل استبدال رسم السيارة) مباشرة في `index.html` مع اختبار بسيط.

ملف الإنشاء: [رسوم-العناصر.md](رسوم-العناصر.md)
